#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)

int main() {
    int n, k;
    vector<int> a(n);
    rep(i, n) {
        cin >> a[i]; 
    }
    int count = 0;
    vector<>
    rep(i, n) {
        
    }
}
